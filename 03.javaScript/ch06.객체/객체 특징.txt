객체  지향
3가지 특징
    1. 상속(Inheritance)
        Student  -> Person을 상속하고, Student의 속성과 메쏘드를 추가

    2. 캡슐화(Encapsulation)
        속성값을 외부에서 직접 접근을 어렵게 하는 것 - 데이터 보호

    3. 다형성(Polymorphism)
        -overloading(x java에서만 사용)

        -overriding() : 상속받은 내용을 자식이 변경

재 사용성이 높아 진다.

